# Этап 1: Извлечение Сущностей
ENTITIES_PROMPT = """
Ты — системный аналитик. Твоя задача — извлечь из текста ключевые **сущности** системы, их **атрибуты** и **состояния**, согласуя их с уже существующим списком.

### СУЩЕСТВУЮЩИЕ СУЩНОСТИ:
{existing_entities}

### ИНСТРУКЦИИ:
1.  **Проверь существующие сущности:** Прежде чем создавать новую сущность, проверь, нет ли в списке выше подходящего синонима.
    *   **Если синоним найден:** Используй **существующее имя** из списка. Например, если в тексте "аккаунт пользователя", а в списке есть "Пользователь", используй "Пользователь".
    *   **Если сущность новая:** Создай новую сущность, если она действительно уникальна и не является синонимом существующих.
2.  **Фокус на сущностях:** Ищи существительные, которые обозначают ключевые объекты системы (например, "Пользователь", "Заказ", "Отчет", "Система").
3.  **Атрибуты и состояния:** Для каждой сущности определи ее характеристики (атрибуты) и возможные состояния.
    *   **Атрибут** — это свойство сущности (например, у "Пользователя" есть "имя", "email").
    *   **Состояние** — это статус, в котором может находиться сущность (например, "Заказ" может быть "создан", "оплачен", "отменен").
4.  **Игнорируй UI и детали реализации:** Пропускай описания интерфейса (кнопки, формы) и технические детали (HTTP-статусы, названия баз данных).
5.  **Игнорируй нерелевантный текст:** Пропускай оглавление, титулы, номера страниц, служебные пометки и декоративные элементы.
6.  **Формат вывода:** Твой ответ должен быть **ТОЛЬКО** валидным JSON объектом. Не добавляй никаких комментариев, объяснений или постороннего текста. Если сущности не найдены, верни пустой JSON `{{"entities": []}}`.
7.  **Самопроверка:** Перед тем как дать финальный ответ, убедись, что сгенерированный тобой JSON синтаксически корректен. Все скобки, кавычки и запятые должны быть на своих местах.
8.  **Обертка:** Оберни финальный JSON в теги `<json>` и `</json>`.

### ПРИМЕР:
-   **Существующие сущности:** `["Пользователь", "Заказ"]`
-   **Текст:** "Зарегистрированный пользователь может создать заявку. После создания заявка находится в статусе 'Новая'. У заявки должен быть уникальный номер и дата создания."
-   **Результат:**
    ```
    <json>
    {{
      "entities": [
        {{
          "name": "Пользователь",
          "description": "Субъект, взаимодействующий с системой.",
          "attributes": [
            {{"name": "статус регистрации", "type": "boolean", "description": "Зарегистрирован ли пользователь"}}
          ]
        }},
        {{
          "name": "Заказ",
          "description": "Заявка на покупку товара.",
          "attributes": [
            {{"name": "номер", "type": "string", "description": "Уникальный идентификатор заказа"}},
            {{"name": "дата создания", "type": "datetime", "description": "Дата и время создания заказа"}}
          ],
          "states": [
            {{"name": "Новый", "description": "Заказ создан, но не обработан"}},
            {{"name": "Оплачен", "description": "Заказ успешно оплачен"}},
            {{"name": "Отменен", "description": "Заказ был отменен"}}
          ]
        }}
      ]
    }}
    </json>
    ```

### ТЕКСТ ДЛЯ АНАЛИЗА:
{text}
"""

# Этап 2: Извлечение Требований
REQUIREMENTS_PROMPT = """
Ты — системный аналитик. Твоя задача — извлечь из текста **технические требования** к системе.

### ИНСТРУКЦИИ:
1.  **Фокус на требованиях:** Ищи фразы, которые описывают, что система **должна делать** (функциональные требования) или какими **свойствами обладать** (нефункциональные).
2.  **Игнорируй UI:** Пропускай описания интерфейса ("кнопка", "форма", "уведомление"). Вместо этого фиксируй суть операции.
3.  **Игнорируй нерелевантный текст:** Пропускай оглавление, титулы, номера страниц, служебные пометки и декоративные элементы.
4.  **Условия и триггеры:** Внимательно ищи слова-маркеры, указывающие на условия или последовательность действий. Если находишь такое слово или фразу, ОБЯЗАТЕЛЬНО добавь его в поле `trigger_words`.
    *   **Пример 1 (прямая зависимость):**
        *   Текст: "После успешной авторизации, система должна отобразить личный кабинет."
        *   Результат:
            ```
            <json>
            {{
              "requirements": [{{
                "text": "Система отображает личный кабинет.",
                "conditions": [{{"condition": "user_status == 'authorized'"}}],
                "trigger_words": ["После успешной авторизации"]
              }}]
            }}
            </json>
            ```
    *   **Пример 2 (косвенная зависимость):**
        *   Текст: "Отправка отчета возможна только для верифицированных пользователей."
        *   Результат:
            ```
            <json>
            {{
              "requirements": [{{
                "text": "Система позволяет отправлять отчет.",
                "conditions": [{{"condition": "user_verification_status == 'verified'"}}],
                "trigger_words": ["только для"]
              }}]
            }}
            </json>
            ```
4.  **Контекст:** Используй предоставленные **сущности и их состояния** для более точного описания условий.
5.  **Формат вывода:** Твой ответ должен быть **ТОЛЬКО** валидным JSON объектом. Не добавляй никаких комментариев, объяснений или постороннего текста. Если требования не найдены, верни пустой JSON `{{"requirements": []}}`.
6.  **Самопроверка:** Перед тем как дать финальный ответ, убедись, что сгенерированный тобой JSON синтаксически корректен. Все скобки, кавычки и запятые должны быть на своих местах.
7.  **Обертка:** Оберни финальный JSON в теги `<json>` и `</json>`.

### СУЩНОСТИ И СОСТОЯНИЯ (КОНТЕКСТ):
{entities_context}

### ТЕКСТ ДЛЯ АНАЛИЗА:
{text}
"""

# Этап 3: Классификация зависимостей между парой требований (для отката)
PAIR_DEPENDENCY_PROMPT = """
Определи тип логической связи между Требованием 1 и Требованием 2.

### СУЩНОСТИ СИСТЕМЫ (КОНТЕКСТ):
{entities_context}

### Требование 1:
"{text1}"

### Требование 2:
"{text2}"

### Типы связей:

- **DEPENDS_ON**: Требование 1 зависит от выполнения или наличия Требования 2.
- **BLOCKS**: Требование 1 блокирует или мешает выполнению Требования 2.
- **RELATES_TO**: Требования связаны по смыслу, но без прямой зависимости.
- **DUPLICATES**: Требования описывают одно и то же.
- **NO_RELATION**: Требования не связаны.

Ответ должен быть одним из типов связи, без объяснений. Если связи нет, верни **NO_RELATION**.
"""

# Этап 3: Пакетная классификация зависимостей
BATCH_DEPENDENCY_PROMPT = """
Ты — системный аналитик. Проанализируй **Требование А** и определи его связи с каждым требованием из **Списка Кандидатов**.

### Требование А:
**ID:** {req_a_id}
**Текст:** "{req_a_text}"

### Список Кандидатов для проверки:
{candidates_list}

### Сущности, упоминаемые в этих требованиях (для контекста):
{relevant_entities}

### ЗАДАЧА:
Для КАЖДОГО кандидата из списка определи тип его связи с Требованием А. 

### Типы связей:
- **DEPENDS_ON**: Требование А зависит от выполнения или наличия кандидата.
- **BLOCKS**: Требование А блокирует или мешает выполнению кандидата.
- **RELATES_TO**: Требования связаны по смыслу, но без прямой зависимости.
- **DUPLICATES**: Требования описывают одно и то же.
- **NO_RELATION**: Требования не связаны.

### ФОРМАТ ВЫВОДА:
1.  Твой ответ должен быть **ТОЛЬКО** валидным JSON объектом.
2.  Включай в список **только** те пары, между которыми есть любая связь, кроме 'NO_RELATION'.
3.  **Самопроверка:** Перед тем как дать финальный ответ, убедись, что сгенерированный тобой JSON синтаксически корректен.
4.  **Обертка:** Оберни финальный JSON в теги `<json>` и `</json>`.

### Пример:
```
<json>
{{
  "dependencies": [
    {{"source": "REQ-001", "target": "REQ-005", "type": "DEPENDS_ON"}},
    {{"source": "REQ-001", "target": "REQ-012", "type": "RELATES_TO"}}
  ]
}}
</json>
```
"""

# Этап 5: Исправление некорректного JSON
FIX_JSON_PROMPT = """
Твоя задача — исправить предоставленный некорректный JSON-объект.

### ИНСТРУКЦИИ:
1.  Проанализируй синтаксис, найди и исправь ошибки (незакрытые скобки, лишние запятые, неправильные кавычки).
2.  **СТРОГИЕ ПРАВИЛА:**
    *   **Никогда не используй двойное двоеточие `::`.** Правильный формат: `"key": "value"`.
    *   **Не разрывай строки внутри кавычек.** Строковые значения должны быть на одной строке.
    *   **Убери все комментарии (`//` или `/* */`).**
3.  Твой ответ должен содержать **ТОЛЬКО** валидный JSON.
4.  Не добавляй никаких комментариев, объяснений или markdown-форматирования.
5.  Оберни финальный JSON в теги `<json>` и `</json>`.

### НЕКОРРЕКТНЫЙ JSON:
{broken_json}

### ИСПРАВЛЕННЫЙ JSON:
<json>
...
</json>
"""