extractor_prompt_template = """
Отвечай на языке запроса.
Ты системный аналитик. Извлеки структурированные **технические требования** из текста. 
Верни ТОЛЬКО JSON-ответ без комментариев, пояснений или метатекста.

### ИНСТРУКЦИИ:
0. СТРОГИЙ ФОРМАТ: только JSON без метатекста
   
1. ПРОПУСТИ эти разделы без анализа:
   - Оглавление, титулы, номера страниц
   - Служебные пометки, декоративные элементы
2. Для оставшегося текста:
   - Идентифицируй **технические требования**, а не описания UI-поведения
   - **Избегай требований вроде "отобразить сообщение", "выделить красным"** — вместо этого фиксируй **что должно произойти**, а не **как это отображается**
   - Пример плохого требования: "Показать уведомление после отправки формы"
   - Пример хорошего: "Система должна подтвердить успешное выполнение операции после отправки формы"
   - В поле requirements.conditions должны быть **семантически значимые условия**, связанные с **состояниями системы** или **бизнес-правилами**, а не низкоуровневые действия (клик, hover, прокрутка). 
     - Плохое условие: "user_interaction == 'click'"
     - Хорошее условие: "user_engaged_with_notification"
   - Если в тексте упоминается состояние системы, добавь его в system_states, а затем используй в условиях. Например:
     - Текст: "Уведомление помечается прочитанным при взаимодействии"
     - system_states: {{"sub_domain": "notification_state", "value": "read"}}
     - requirements.conditions: {{"condition": "notification_state == 'read'"}}
   - Избегай деталей вроде HTTP-статусов (200, 404) — используй общие термины вроде "успешный ответ", "ошибка валидации", "неавторизованный доступ"
   - Пример хорошего требования: "Система должна вернуть ошибку валидации, если email не соответствует формату"
   - Извлеки состояния системы, события, условия, описания сущностей системы
   - Выяви зависимости между требованиями
   - Если в тексте несколько требований — верни их в виде массива в поле "requirements"
   - Формат вывода: строго JSON
3. Если текст НЕ содержит требований (например, только оглавление) - верни пустой JSON

### ТЕКСТ:
{text}

### ФОРМАТ ВЫВОДА:
{{
  "requirements": [
    {{
      "text": "Пользователь может отменить заказ до оплаты",
      "domain": "orders",
      "type": "functional",
      "states": ["order_created", "order_cancelled"],
      "conditions": [
        {{
          "condition": "payment_status == 'pending'",
        }}
      ]
    }}
  ],
  "entities": [
    {{
      "name": "Заказ",
      "description": "Основная бизнес-сущность системы...",
      "attributes": [
        {{"name": "Номер", "type": "string"}},
        {{"name": "Сумма", "type": "decimal"}}
      ]
    }}
  ],
  "system_states": [
    {{
      "sub_domain": "payment_status",
      "value": "pending",
      "name": "payment_pending",
      "description": "Ожидание подтверждения платежа",
    }}
  ]
}}
"""

semantic_relation_prompt_template = """
Определи тип связи между двумя требованиями:

Требование 1: "{text1}"
Требование 2: "{text2}"

Возможные типы:
- DEPENDS_ON: Одно требование зависит от выполнения другого
- CONFLICTS_WITH: Требования противоречат друг другу
- RELATED_TO: Общая тематика, но нет прямой зависимости
- DUPLICATES: Дублирующиеся требования
- TRIGGERS: Одно требование вызывает выполнение другого

Ответ должен быть одним словом из списка выше, без объяснений.
"""